 
This is the professional project in the backend.
- model link https://app.eraser.io/workspace/YtPqZ1VogxGy1jzIDkzj

<!-- create the .gitkeep file  into the empty folder to keep and push the empty folders to the git remember -->

nodemon --server get restarted when new changes or saved are done
dev dependencies == dependencies which are used during the development and not used in the productions
dependencies == 
{
    "singleQuote": false,
    "bracketSpacing": true,
    "tabWidth": 2,
    "trailingComma": "es5",
    "semi": true
} 
//prettier   === dev dependency
// this is the standard practice used in the development for the project ,to keep all the changes same for all and to prevent the conflict genetratrd during the file merging
//npm i mongoose express dotenv
//"nodemon -r dotenv/config --experimental-json-modules src/index.js"  we can load environmental variables in the nodemon by using this command